import { Action } from 'redux';
//login reducer

export const loginReducer = (state={}, action : Action) => {
    switch (action.type) {
        case 1:
            
            break;
    
        default:
            return {...state};
    }
}