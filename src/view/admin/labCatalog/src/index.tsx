import * as React from 'react';

interface ICatalogProps {
}

const CatalogComponent: React.FunctionComponent<ICatalogProps> = (props) => {
  return(
      <div> this is a catalog component</div>
  ) ;
};

export default Catalog;